# frozen_string_literal: true

source "https://rubygems.org"

gem 'ostruct'       # Required for Ruby 4.0+ compatibility
gem 'securerandom', '~> 0.4.0'
gem 'prompt_manager', path: '../../prompt_manager'  # Local PM v1.0.0 during development

group :development do
  gem 'aigcm'
  gem 'debug_me'
  gem 'simplecov_lcov_formatter'
  gem 'deep-cover', require: false
end

gemspec
