The Ruby version is <%= RUBY_VERSION %>.
The current time is <%= Time.now.strftime('%H:%M:%S') %>.
There are <%= Dir.glob('prompts_dir/*.md').size %> prompt files in the prompts directory.

Summarize the info above in one sentence.
